import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // PPR is canary-only — opt back in once on a canary build of Next 15.
  // experimental: { ppr: true },
};

export default nextConfig;
